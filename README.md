# Vox Pupuli base containers

base container as building blocks for other containers.
These containers are designed to be used as a base for other containers,
and are not intended to be run directly.
